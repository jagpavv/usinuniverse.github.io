---
title: 회원 가입
layout: post
hide: true
---

# 여덟번째 연습 - 회원 가입(커넥트 재단 - iOS 부스트 코스)

### UI 구현

[![SignUp](https://github.com/usinuniverse/usinuniverse.github.io/blob/master/assets/images/project%20images/09/01.png?raw=true)](https://vimeo.com/272285870)

* 클릭하시면 동영상을 감상하실 수 있습니다.

### 요구사항

### 화면 1

1. 화면구성

* 아이디와 패스워드를 입력하는 필드가 있고, 각각 알맞은 플레이스홀더(placeholder)가 적혀있습니다.
* 상단에는 사이트와 관련된 이미지를 보여주며, 그 아래에는 로그인 버튼과 회원가입 버튼이 있습니다.

2. 기능

* 로그인 버튼은 눌러도 아무 반응이 없으며 회원가입 버튼을 누르면 화면2로 전환됩니다.

### 화면 2

1. 화면구성
* 상단 오른쪽에는 아이디, 패스워드, 패스워드 확인을 하는 필드가 있고, 각각 알맞은 플레이스홀더(placeholder)가 적혀있습니다.
* 상단 왼쪽에 이미지뷰가 있고, 그 아래에는 자기소개를 위한 텍스트뷰가 위치합니다.
* 텍스트뷰 아래의 왼쪽에는 '취소'버튼이 위치하며, 그 오른쪽에는 '다음'버튼이 위치합니다.

2. 기능

* 상단 왼쪽의 이미지뷰를 탭하면 UIImagePickerViewController가 뜨고, 이미지를 간단히 편집해 프로필 사진으로 선택할 수 있습니다.
* 프로필 이미지뷰는 정사각형이며, 이미지뷰 내부에 보이는 이미지는 이미지 원래의 비율을 유지합니다.
* 화면 중간의 텍스트 뷰에서 자기소개를 작성할 수 있습니다.
* 화면 왼쪽 하단의 '취소' 버튼을 누르면 모든 정보가 지워지고 이전 화면1로 되돌아갑니다.
* 사용자가 모든 정보를 기입한 상태가 아니라면 화면 오른쪽 하단의 '다음' 버튼은 기본적으로 비활성화되어있으며, 프로필 이미지, 아이디, 자기소개가 모두 채워지고, 패스워드가 일치하면 '다음' 버튼이 활성화됩니다.

### 화면 3

1. 화면구성
* 화면 상단에는 전화번호를 입력할 수 있는 텍스트 필드가 있습니다.
* 텍스트 필드 하단에는 선택한 생년월일을 표시할 수 있는 레이블이 있으며 그 하단에는 날짜를 선택할 수 있는 피커가 있습니다.
* 피커 하단에는 '이전', '취소', '가입' 버튼이 위치합니다.

2. 기능
* 사용자가 모든 정보를 기입한 상태가 아니라면 '가입' 버튼은 기본적으로 비활성화되어있습니다. 전화번호와 생년월일이 채워지면 '가입' 버튼이 활성화됩니다. 또, 활성화된 '가입' 버튼을 선택하면 화면1로 되돌아가고, 가입한 아이디가 화면1의 아이디 필드에 입력되어있습니다.
* '이전' 버튼을 누르면 현재 정보를 저장해두고 화면2로 돌아가며, '취소' 버튼을 누르면 모든 정보가 지워지고 화면1로 돌아갑니다.
* 전화번호 필드를 누르면 숫자키패드가 올라옵니다.
* 생년월일 선택은 UIDatePicker를 활용합니다.
* UIDatePicker의 숫자가 바뀌면 생년월일 레이블에 즉각 반영됩니다.

### 추가 사항

* 애플리케이션 구현에 필요한 싱글턴 인스턴스를 생성하여 활용합니다. 싱글턴 클래스 이름은 UserInformation으로 합니다.

### 코드

* SignInViewController.swift

```swift
import UIKit



class SignInViewController: UIViewController {
    
    // MARK: - Properties
    // MARK: IBOutlet
    
    @IBOutlet weak var idTextField: UITextField!
    @IBOutlet weak var passwordTextField: UITextField!
    @IBOutlet weak var signInButton: UIButton!
    @IBOutlet weak var signUpButton: UIButton!
    
    
    
    // MARK: - Methods
    // MARK: View Life Cycle
    
    override func viewDidLoad() {
        super.viewDidLoad()
        
        self.signInButton.isEnabled = false
        
        self.idTextField.tag = 1
        self.idTextField.delegate = self
        self.passwordTextField.tag = 2
        self.passwordTextField.delegate = self
    }
    
    override func viewWillAppear(_ animated: Bool) {
        super.viewWillAppear(true)
        
        if let id = UserInformation.shared.id {
            self.idTextField.text = id
        }
    }
    
    // MARK: Memory Management
    
    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    
    // MARK: Framework
    
    override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
        self.view.endEditing(true)
    }
    
    // MARK: Custom
    
    func signInConditionCheck() {
        if self.idTextField.text?.isEmpty == false && self.passwordTextField.text?.isEmpty == false {
            self.signInButton.isEnabled = true
        } else {
            self.signInButton.isEnabled = false
        }
    }
    
    // MARK: IBAction
    
    @IBAction func didChangeIdTextField(_ sender: UITextField) {
        signInConditionCheck()
    }
    
    @IBAction func didChangePasswordTextField(_ sender: UITextField) {
        signInConditionCheck()
    }
    
}

// MARK: - Extionstion

// MARK: UI Text Field Delegate

extension SignInViewController: UITextFieldDelegate {
    
    func textFieldShouldReturn(_ textField: UITextField) -> Bool {
        if textField.tag == 1 {
            self.passwordTextField.becomeFirstResponder()
        } else if textField.tag == 2 {
            self.view.endEditing(true)
        }
        
        return true
    }
    
}



```

* SignUpViewController.swift

```swift
import UIKit
import Photos



class SignUpViewController: UIViewController {
    
    // MARK: - Properties
    // MARK: Custom
    
    let imagePicker = UIImagePickerController()
    
    // MARK: IBOutlet
    
    @IBOutlet weak var idTextField: UITextField!
    @IBOutlet weak var passwordTextField: UITextField!
    @IBOutlet weak var rePasswordTextField: UITextField!
    @IBOutlet weak var introduceTextView: UITextView!
    @IBOutlet weak var profileImageView: UIImageView!
    @IBOutlet weak var nextButton: UIButton!
    
    
    
    // MARK: - Methods
    // MARK: View Life Cycle
    
    override func viewDidLoad() {
        super.viewDidLoad()
        
        self.nextButton.isEnabled = false
        self.navigationController?.isNavigationBarHidden = true
        
        self.idTextField.tag = 1
        self.idTextField.delegate = self
        self.passwordTextField.tag = 2
        self.passwordTextField.delegate = self
        self.rePasswordTextField.tag = 3
        self.rePasswordTextField.delegate = self
        
        self.introduceTextView.delegate = self
        
        self.imagePicker.delegate = self
        self.imagePicker.sourceType = .photoLibrary
        self.imagePicker.allowsEditing = true
        
        let imageViewTapRecognizer = UITapGestureRecognizer(target: self, action: #selector(self.presentProfileImagePicker))
        self.profileImageView.addGestureRecognizer(imageViewTapRecognizer)
        
        let viewTapRecognizer = UITapGestureRecognizer(target: self, action: #selector(self.didTap))
        self.view.addGestureRecognizer(viewTapRecognizer)
    }
    
    // MARK: Memory management
    
    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    
    // MARK: Custom
    
    @objc func presentProfileImagePicker() {
        PHPhotoLibrary.requestAuthorization { (status) in
            switch status {
            case .authorized:
                self.present(self.imagePicker, animated: true, completion: nil)
            case .notDetermined:
                print("결정되지 않음")
            case .denied:
                print("거부됨")
            case .restricted:
                print("제한됨")
            }
        }
    }
    
    @objc func didTap() {
        self.view.endEditing(true)
    }
    
    func signUpConditionCheck() {
        if self.idTextField.text?.isEmpty == false && self.passwordTextField.text?.isEmpty == false && self.rePasswordTextField.text?.isEmpty == false && self.profileImageView.image != nil && self.introduceTextView.text.isEmpty == false {
            guard self.passwordTextField.text == self.rePasswordTextField.text else {
                self.nextButton.isEnabled = false
                return
            }
            self.nextButton.isEnabled = true
        } else {
            self.nextButton.isEnabled = false
        }
    }
    
    // MARK: IBAction
    
    @IBAction func didChangeIdTextField(_ sender: UITextField) {
        signUpConditionCheck()
    }
    
    @IBAction func didChangePasswordTextField(_ sender: UITextField) {
        signUpConditionCheck()
    }
    
    @IBAction func didChangeRePasswordTextField(_ sender: UITextField) {
        signUpConditionCheck()
    }
    
    @IBAction func didTapCancelButton(_ sender: UIButton) {
        UserInformation.shared.phoneNumber = nil
        UserInformation.shared.birthday = nil
        UserInformation.shared.id = nil
        UserInformation.shared.password = nil
        UserInformation.shared.profileImage = nil
        
        self.presentingViewController?.dismiss(animated: true, completion: nil)
    }
    
    @IBAction func didTapNextButton(_ sender: UIButton) {
        UserInformation.shared.id = self.idTextField.text
        UserInformation.shared.password = self.passwordTextField.text
        UserInformation.shared.profileImage = self.profileImageView.image
    }
}

// MARK: - Extension

// MARK: UI ImagePicker Delegate

extension SignUpViewController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    
    func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [String : Any]) {
        if let image = info[UIImagePickerControllerEditedImage] as? UIImage {
            self.profileImageView.image = image
            self.signUpConditionCheck()
        } else if let image = info[UIImagePickerControllerOriginalImage] as? UIImage {
            self.profileImageView.image = image
            self.signUpConditionCheck()
        }
        picker.dismiss(animated: true, completion: nil)
    }
    
}

// MARK: UI TextField Delegate

extension SignUpViewController: UITextFieldDelegate {
    
    func textFieldShouldReturn(_ textField: UITextField) -> Bool {
        if textField.tag == 1 {
            self.passwordTextField.becomeFirstResponder()
        } else if textField.tag == 2 {
            self.rePasswordTextField.becomeFirstResponder()
        } else if textField.tag == 3 {
            self.introduceTextView.becomeFirstResponder()
        }
        
        return true
    }
    
}

// MARK: UI TextView Delegate

extension SignUpViewController: UITextViewDelegate {
    
    func textViewDidChange(_ textView: UITextView) {
        self.signUpConditionCheck()
    }
    
}




```

* PersonalInformationViewController.swift

```swift
import UIKit



class PersonalInfomationViewController: UIViewController {
    
    // MARK: - Properties
    // MARK: Custom
    
    let dateFormatter = DateFormatter()
    
    // MARK: IBOutlet
    
    @IBOutlet weak var phoneNumberTextField: UITextField!
    @IBOutlet weak var birthdayLabel: UILabel!
    @IBOutlet weak var birthdayPicker: UIDatePicker!
    @IBOutlet weak var signUpButton: UIButton!
    
    
    
    // MARK: - Methods
    // MARK: View Life Cycle
    
    override func viewDidLoad() {
        super.viewDidLoad()
        
        self.dateFormatter.dateFormat = "yyyy-MM-dd"
        
        self.birthdayLabel.text = dateFormatter.string(from: self.birthdayPicker.date)
        self.signUpButton.isEnabled = false
        
        self.navigationController?.isNavigationBarHidden = true
        
        
    }
    
    override func viewWillAppear(_ animated: Bool) {
        super.viewWillAppear(true)
        
        if let phoneNumber = UserInformation.shared.phoneNumber {
            self.phoneNumberTextField.text = phoneNumber
        }
        
        if let birthday = UserInformation.shared.birthday {
            self.birthdayLabel.text = birthday
            if let date = self.dateFormatter.date(from: birthday) {
                self.birthdayPicker.setDate(date, animated: false)
            }
        }
        
        signUpConditionCheck()
    }
    
    // MARK: Memory Management
    
    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    
    // MARK: Framework
    
    override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
        self.view.endEditing(true)
    }
    
    // MARK: Custom
    
    func signUpConditionCheck() {
        if self.phoneNumberTextField.text?.isEmpty == false && self.birthdayLabel.text?.isEmpty == false {
            self.signUpButton.isEnabled = true
        } else {
            self.signUpButton.isEnabled = false
        }
    }
    
    // MARK: IBAction
    
    @IBAction func didTapCancelButton(_ sender: UIButton) {
        UserInformation.shared.phoneNumber = nil
        UserInformation.shared.birthday = nil
        UserInformation.shared.id = nil
        
        self.dismiss(animated: true, completion: nil)
    }
    
    @IBAction func didTapPreviousButton(_ sender: UIButton) {
        if let phoneNumber = self.phoneNumberTextField.text {
            UserInformation.shared.phoneNumber = phoneNumber
        }
        
        if let birthday = self.birthdayLabel.text {
            UserInformation.shared.birthday = birthday
        }
        
        self.navigationController?.popViewController(animated: true)
    }
    
    @IBAction func didTapSignUpButton(_ sender: UIButton) {
        UserInformation.shared.phoneNumber = self.phoneNumberTextField.text
        UserInformation.shared.birthday = birthdayLabel.text
        
        self.dismiss(animated: true, completion: nil)
    }
    
    @IBAction func didChangePhoneNumberTextField(_ sender: UITextField) {
        self.signUpConditionCheck()
    }
    
    @IBAction func didChangeBirthdayPickerValue(_ sender: UIDatePicker) {
        self.birthdayLabel.text = dateFormatter.string(from: sender.date)
        
        self.signUpConditionCheck()
    }
    
}



```

* userInformation.swift

```swift
import Foundation
import UIKit



final class UserInformation {
    static let shared = UserInformation()
    
    var id: String?
    var phoneNumber: String?
    var birthday: String?
    var password: String?
    var profileImage: UIImage?
}



```
